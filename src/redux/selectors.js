export const getContacts = (state) => state.contacts.items;

export const getFilter = (state) => state.filter.name;

export const getCategory = (state) => state.contacts.items.category;

export const getState = (state) => state.contacts;
